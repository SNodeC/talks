\subsection{WebSocket Application}

\begin{frame}[fragile]{WebSocket Application}
  \begin{cppcode}[autogobble=true, caption={WebSocket Application.},captionpos=b]
    #include "express/legacy/WebApp.h"
    #include <cstring>

    int main(int argc, char* argv[]) {
      express::WebApp::init(argc, argv);

      express::legacy::WebApp webApp;

      webApp.get("/", [](express::Request& req, express::Response& res) -> void {
        if (!res.upgrade(req)) {
          res.end();
        }
      });

      webApp.listen(8080, [](const core::ProgressLog& progressLog) -> void {
        progressLog.logProgress();
      });

      return express::WebApp::start();
    }
  \end{cppcode}
\end{frame}
