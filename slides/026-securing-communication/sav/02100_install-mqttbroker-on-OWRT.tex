\begin{frame}{Installiton of the \alert{\texttt{mqttbroker}} Package on the Router}
{\href{https://github.com/SNodeC/mqttbroker}{\beamergotobutton{https://github.com/SNodeC/mqttbroker}}}
\begin{exampleblock}{Tasks (group): Install \texttt{mqttbroker} on the router}
\begin{enumerate}
  \item Use the \alert{WEB interface} or the \alert{command line} to install the \alert{\texttt{mqttbroker}} package.
  \begin{description}[???]
  \item[$\Rightarrow$] This package contains the two MQTT v3.1.1 conform \alert{mqttbroker} (replacement for mosquitto) and \alert{mqttintegrator} (replacement for Node-RED) applications.
  \item[???] Are the \texttt{mqttbroker} and \texttt{mqttintegrator} applications running after install?
  \end{description}
  \item Start \texttt{mqttbroker} on the command line. If it is already running as a daemon stop the daemon.
  \begin{description}[???]
  \item[???] What happens?
  \end{description}
\end{enumerate}
\end{exampleblock}
\end{frame}
