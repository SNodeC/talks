\subsection{Network Layer and Databases}

\begin{frame}{Network Layer and Databases}
   \begin{itemize}
     \item \alert{\texttt{SocketServer}} and \alert{\texttt{SocketClients}} classes \alert{(\texttt{ssl/tls}} and \alert{\texttt{legacy}} versions) for:
     \begin{description}[\texttt{MariaDBClient}]
       \item [\texttt{IPv4}] Must have
       \item [\texttt{IPv6}] Dual stack, single stack, IPv4 mapped addresses, \ldots
       \item [\texttt{RFCOMM}] Bluetooth emulated serial line (RS-232). \alert{Reliable} connection.
       \item [\texttt{L2CAP}] Base bluetooth layer (\alert{L}ayer \alert{2} En\alert{cap}sulation)
       \item [\texttt{Unix Domain}] Local communication. Socket address is path (sun-path) in filesystem.
     \end{description}
     \item \alert{Database} support
     \begin{description}[\texttt{MariaDBClient}]
        \item [\texttt{MariaDBClient}] Ready for use
       \begin{itemize}
         \item Queries and execs
         \item Transactions
       \end{itemize}
     \end{description}
   \end{itemize}
\end{frame}
