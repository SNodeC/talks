\begin{frame}[fragile]{Beispiel der Verwendung des express-API}
  \begin{cppcode}[autogobble=true]
    #include "express/legacy/WebApp.h"
    #include <cstring>

    int main(int argc, char* argv[]) {
      express::WebApp::init(argc, argv);

      express::legacy::WebApp webApp;

      webApp.get("/", [](express::Request& req, express::Response& res) -> void {
        res.sendFile("/home/voc/projects/ServerVoc/docs/html" + req.url, [&req](int ret) -> void {
          if (ret != 0) {
            PLOG(ERROR) << req.url;
          }
        });
      });

      // further routes can be defined

      webApp.listen(8080, [](const core::ProgressLog& progressLog) -> void {
        progressLog.logProgress();
      });

      return express::WebApp::start();
    }
  \end{cppcode}
\end{frame}
